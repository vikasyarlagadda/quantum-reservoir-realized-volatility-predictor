# Virtual environment
.venv/
venv/

# Python cache
__pycache__/
*.pyc
*.pyo

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# VS Code local settings
.vscode/

# ── Personal / repo-owner-only files ─────────────────────────
# Claude project instructions (not for public distribution)
CLAUDE.md

# Claude Code configuration directory
.claude/

# Personal reproduction documentation
docs/

# ── Generated data (recreated by running preprocess.py) ──────
# data/ is included in version control

# ── Generated results (recreated by running the notebooks) ───
# results/ is included in version control

# ── Legacy paths (kept for safety in case of stale clones) ───
Classical_Reservoir_learning/
LSTM/
Data_raw.csv
dff.csv
output.csv
predict_results.csv
# Root-level model plots and stat files (canonical location: results/)
/*_model.png
/dm_stat.csv
/p_value.csv

# Recomputable quantum operators and reservoir features. These remain on disk;
# manifests/checkpoints/results are retained. See MODERNIZATION_AUDIT.md.
results/*/cache/
.cache/

# Raw provider payloads/daily prices stay local; publish retrieval provenance,
# derived monthly features and research results (see MODERNIZATION_AUDIT.md).
data/snapshots/*/yahoo.json
data/snapshots/*/fred.csv
data/snapshots/*/daily.csv
data/snapshots/*/crosscheck.csv
data/snapshots/2026-09-14/

# Preserve diagnostic runs and resumable per-origin records locally. Published
# aggregate forecasts carry all model/origin results and regenerate reports.
results/modern-pilot*/
results/validation-*/
results/*/checkpoints/
results/legacy-2026-09-14/workspace/
